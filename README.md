# Qooxdoo form demo page

A form demo used in different demo apps


## Usage

```
$ qx pkg update
$ qx pkg install qooxdoo/qxl.formdemo
$ qx serve 
```

```
    var formItems = new qxl.formdemo.FormItems();
    this._widgets = formItems.getWidgets();
    this.add(formItems);
```
